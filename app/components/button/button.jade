mixin button (data, options)
  - options = setOptionsDefaults(options, "button", components.button)
  - var classes = getOptionsModifier(options, "button", null, components.button)

  a.button(class=classes, href=data.href)= data.text
